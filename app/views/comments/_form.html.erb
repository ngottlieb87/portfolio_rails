<%= render "layouts/errors", :object => @comment %>

<%= form_for [@project, @comment] do |f| %>
  <%= f.label :body %><br>
  <%= f.text_area :body %><br>
  <%= f.hidden_field(:user_id, :value => current_user.id) %>
  <%= f.submit %>
<% end %>

<p><%= link_to "Return to comments", project_path(@project.id) %></p>
